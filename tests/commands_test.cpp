#include "board.hpp"
#include "catch.hpp"
#include "chess.hpp"
#include "commands.hpp"

TEST_CASE("", "[commands]") {
    //chess::board board;
    //chess::command_handler command_handler;
    //
    //board[0][0].piece(chess::piece::PAWN);
    //REQUIRE(board[0][0].piece() == chess::piece::PAWN);
    //
    //REQUIRE_FALSE(board == chess::board());
    //
    //chess::command command(chess::command_type::RESET, {});
    //command_handler.handle(command, board);
    //
    //REQUIRE(board == chess::board());
}
